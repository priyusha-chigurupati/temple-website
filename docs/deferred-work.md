# Deferred Work Tracker

This file tracks the items we intentionally postponed so they stay visible after the main build moves forward.

## Global

- Run a full end-to-end QA pass across all public and admin forms after the main build is complete.
- Add public/admin multilingual workflows after the English-first build is fully stable.

## Home

- Finalize the Donations preview workflow before reconnecting Quick Contribution behavior.
- Revisit footer-specific refinements later.

## Donations

- Reuse the white space near the instruction block for a future content area such as FAQs, reassurance copy, or receipt notes.
- Decide whether donation notifications should also trigger email alerts.

## Gallery

- Decide whether approved gallery submissions should automatically create Media Library records or stay as a separate reviewed queue until manually published.
- Revisit modal polish after the admin media flow is complete.
- Revisit the admin Media Library layout density so the page uses space more efficiently and large empty areas are reduced.
- Clarify the Media Library filter UI so the status filters and category filters are more clearly labeled.

## Admin

- Revisit sidebar user email display so the full address can be inspected more easily without harming the layout.
- Revisit overall admin page layout density so summary cards, filters, and content blocks use space more cleanly across modules like Media Library and Blog.
- Connect the admin notification settings to real SMTP delivery once the final Hostinger or Zoho credentials are available.
- Replace the local forgot-password reset link display with real outgoing reset emails after SMTP is configured.
- Continue building the remaining admin operations:
  - Final QA across the new submission-management screens
  - Optional export and archive tools for contact, donations, gallery submissions, and newsletter records
